import Graph from "./Graph";

const Main = ({}) => {
    return (
        <div className="Main">
            <Graph
                height={window.innerHeight}
                width={window.innerWidth}
            />
        </div>
    )
}

export default Main;
